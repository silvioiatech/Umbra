[build]
dockerfile = "services/umbra/Dockerfile"
context = "."

[deploy]
numReplicas = 1
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"

# Force Railway to use the multi-service configuration from railway.json
# This prevents Nixpacks auto-detection and ensures all 6 services are deployed
