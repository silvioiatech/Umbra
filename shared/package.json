{
  "name": "@umbra/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Umbra Bot system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "build:clean": "rm -f tsconfig.tsbuildinfo && tsc --build",
    "dev": "tsc --watch",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "uuid": "^9.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "@types/express": "^4.17.0",
    "@types/multer": "^1.4.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}